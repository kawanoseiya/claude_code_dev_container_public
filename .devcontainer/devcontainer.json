{
  "name": "Claude Code Dev Container",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "features": {
    "ghcr.io/anthropics/devcontainer-features/claude-code:1.0.5": {}
  },
  "customizations": {
    "vscode": {
      "settings": {
        "terminal.integrated.defaultProfile.linux": "bash"
      }
    }
  },
  "postCreateCommand": "export PATH=$(npm bin -g):$PATH && echo $PATH && claude-code --version || echo 'Claude Code CLI not found in PATH. Consider linking manually.'",
  "remoteUser": "vscode"
}
